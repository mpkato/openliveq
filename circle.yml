machine:
  python:
      version: 3.5.2
dependencies:
    pre:
        - pip install --upgrade setuptools
        - pip install --upgrade pip
        - pip install pytest
test:
  override:
    - py.test
